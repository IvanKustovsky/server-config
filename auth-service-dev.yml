keycloak:
  realm: ${KEYCLOAK_REALM:e2rent_dev}
  domain: ${KEYCLOAK_DOMAIN:localhost:8020}
  url: ${KEYCLOAK_URL:http://${KEYCLOAK_DOMAIN:localhost:8020}}
  restApiClientId: ${KEYCLOAK_REST_API_CLIENT_ID}
  restApiClientSecret: ${KEYCLOAK_REST_API_CLIENT_SECRET}