spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rentapp
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password

logging:
  level:
    com:
      example:
        user_service: INFO